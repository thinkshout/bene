langcode: en
status: true
dependencies:
  config:
    - field.storage.node.bene_header_media
    - media_entity.bundle.image
    - node.type.page
id: node.page.bene_header_media
field_name: bene_header_media
entity_type: node
bundle: page
label: 'Header image'
description: 'a. Library: is composed of images you have already uploaded. 
b. Upload: (can be enabled or disabled for different user permissions) 
You can search your computer for an image and upload it here. 
You can utilize the white plus sign over the image to select a focal point for it’s display on the site. 
c. Embed: This is a link to an offsite image or video. You will just need the url.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:media'
  handler_settings:
    target_bundles:
      image: image
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
